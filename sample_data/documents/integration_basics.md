# 医疗系统接口基础（合成）

系统集成常通过 HTTP API、消息队列或标准消息交换。接口契约应明确字段、版本、幂等键、超时、重试和错误码。重试必须有次数上限和退避，避免无限循环。

上线前应在合成数据环境验证正常请求、非法参数、重复请求、下游超时和回滚路径。监控至少包含请求量、错误率、P50/P95 延迟和队列深度。

所有接口日志必须脱敏。租户过滤应在数据库查询和检索阶段完成，而不是在模型生成答案后再过滤。
