# EMR 运维手册（合成）

EMR 是电子病历系统。本资料不提供医疗诊断，仅用于说明合成环境的 IT 运维方法。

## 文书保存失败

检查应用错误码、数据库连接、对象存储状态、模板服务和电子签名服务。排查日志时使用脱敏后的文书编号，不复制病历正文。若错误发生在发布之后，先比对版本与配置，再决定是否回滚。

## 审计要求

关键操作记录操作者、租户、请求 ID、资源、结果与时间。审计日志禁止包含原始病历、密码和访问令牌。
